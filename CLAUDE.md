# Courdeck — 開発時の指針

非営利の公開ツール。営業用のサンプルではない。**幅広く使ってもらうこと**が目的なので、
判断に迷ったら「見た目の良さ」より「使える人が増えるか」「規約を守れているか」を優先する。

## 構成

```
index.html    アプリ本体。これ1枚で動く（HTML/CSS/JS、依存パッケージなし）
anime.html    旧URL用のリダイレクト
data/streaming.json          国内の配信情報。GitHub Actions が生成する
scripts/fetch-streaming.mjs  上記の取得スクリプト
scripts/serve.mjs            手元で確認するための静的サーバー
```

## 守ること

**外部リクエストを増やさない。** Google Fonts・解析・広告・CDNのライブラリは入れない。
フッターに「利用者の情報は一切収集しません」と明記しており、外部への通信を足すとこれが嘘になる。
装飾が必要ならインラインSVGとシステムフォントで作る。

**アプリ本体は単一HTMLを保つ。** ビルド工程を持ち込まない。閲覧者に必要なのは
`index.html` を開くことだけ、という状態を崩さない。

**商用利用しない。** AniList・TMDB双方の規約で禁じられている。広告収入や集客目的も
商用と見なされる点に注意。

## データ源と規約（重要）

| 提供元 | 制約 |
| --- | --- |
| **AniList** | 非営利のみ。**競合するリスト/トラッカーサービスとしての利用は禁止**。視聴話数の記録やリスト共有を足すとこの線に近づくので、追加前に要検討。アプリ名に "AniList" は使えない。30リクエスト/分。`isAdult:false` で除外必須 |
| **TMDB** | 非営利のみ。**コンテンツの再許諾（sublicense）禁止**。ロゴ掲示と指定文言の表示が義務。キャッシュは6か月まで |
| **Wikipedia** | CC BY-SA 4.0。記事ごとに出典・記事名・ライセンスへのリンクを表示する |
| **Fribb/anime-lists**<br>← anime-offline-database | **ODbL 1.0**。継承条項があり、TMDBの再許諾禁止と衝突する |

### 特に壊してはいけない設計

`data/streaming.json` に **TMDBのIDを書き出さない**。ID対応表はビルド中の照合にのみ使う。
ODbLは派生データベースの同一ライセンス公開を求めるが、TMDBは再許諾を禁じており、
両方を同じファイルに載せると矛盾する。IDを持ち出さないことで衝突を回避している。

自前照合のキャッシュ（既定 `.cache/tmdb-ids.json`）も **TMDBのIDを持つのでリポジトリに入れない**。
gitignore 済みで、GitHub Actions では `actions/cache` に載せている。`data/` へ移したり公開物に
混ぜたりしないこと。失われても1回150件の上限つきで引き直されるだけなので、
「消えると困るからリポジトリに置く」という判断はしない。

TMDBのAPIキーは GitHub Secrets（`TMDB_API_KEY`）に置く。公開物には絶対に含めない。
ローカル実行時は `.tmdb_key`（gitignore済み）から読む。

規約の注意文は**一字一句、規約が指定するとおり**に表示する。
`This application uses TMDB and the TMDB APIs but is not endorsed, certified, or otherwise approved by TMDB.`

## コードの約束

**多言語化**: 静的テキストは `data-i18n` / `data-i18n-ph` / `data-i18n-title` 属性、
動的な文字列は `t("key", {vars})`。**辞書は `ja` と `en` の両方に必ず足す**。
文言を直書きしない（英語表示に日本語が混ざる事故が実際に起きた）。

**`t` という名前のローカル変数を作らない。** 翻訳関数 `t()` を隠して、その関数内で
文言を扱った瞬間に落ちる。

**APIから来た文字列は必ず `esc()` を通す。** 説明文は `plain()` でタグを除去してから
テキストとして入れる。URLパラメータは `validOpt()` で選択肢と照合する。

**読み込んだファイルはAPI以上に信用しない。** お気に入りの読み込みは `favClean()` を通し、
既知の項目だけを型を見ながら移し替える。URLは http(s) のみ、色は16進のみ、未知の項目は捨てる。
利用者が自分で作った JSON を読ませられる以上、描画へ流れ込む値をここで絞る。
形式のバージョンは `FAV_FORMAT`。互換性を壊す変更をしたら必ず上げる。

**放送日は朝5時始まり。** 深夜帯は24時制（25:30 = 翌1:30）。日付をまたぐ処理は
`index.html` の `/* date-logic:start */` 〜 `/* date-logic:end */` の区間にまとめてあり、
各所で書き直さない。この区間は**DOMもグローバル状態も参照しない**。現在時刻に依存する関数は
基準時刻を引数で受け取る（`broadcastToday(now)` / `calWeekStart(now, offset)`）。
テストがこの区間だけを切り出して読み込むので、区切りコメントを消さないこと。また区間内で
`new Date()` を引数なしに呼ぶとテストが時刻を固定できなくなるので、そこは呼び出し側に置く
（`calStart()` がその例）。

**プレビューをフォーカスで開くのはキー操作のときだけ。** マウスでカードを押してもフォーカスは
当たるので、`keyNav`（直前が Tab / 矢印キーだったか）を見ずに `focusin` で開くと、クリックの
たびにパネルが一瞬出る。ボタンから開いたときだけ `pinned` を付けて操作可能にし、閉じるときに
必ず外す。付けたままだと透明なパネルがタップを横取りする。

**キャッシュキーはクエリの指紋を含む。** `MEDIA_FIELDS` にフィールドを足せば自動的に
キーが変わる。この仕組みが無かったとき、古い形のデータを6時間掴み続ける不具合が出た。

## 確認方法

```
node scripts/serve.mjs      # → http://localhost:8765
```

**`file://` で開くと配信情報だけ読み込めない**（ブラウザがローカルファイルへの `fetch` を
禁じるため）。公開時と同じ状態を見るには必ずサーバー経由で確認する。

変更後に見る箇所: 一覧・曜日表・お気に入りの3タブ、詳細モーダル、日本語と英語の両方、
キーボード操作（Tab → Enter で詳細、`/` で検索、Esc で復帰）、モバイル幅（375px）。

## 配信情報の更新

```
node scripts/fetch-streaming.mjs        # 前季〜来季＋歴代トップ（既存にマージ）
node scripts/fetch-streaming.mjs --all  # 全作品（全置換・約6分）
```

GitHub Actions が毎日03:00 JSTに前者、日曜03:30 JSTに後者を実行する。

## コミットメッセージ

日本語で書く。**「何を変えたか」だけでなく「なぜそうしたか」を残す。**
規約や外部仕様が理由の場合は特に、後から読んで判断の根拠が分かるようにする。
